language: julia

os:
  - linux
  - osx
  - windows

julia:
  - 1.1
  - 1.3

branches:
  only:
    - master
    - dev

notifications:
  email: false

jobs:
  include:
    - stage: "Doc and code cov."
      julia: 1.1
      os: linux
      script:
        - julia --project=docs/ -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd()));
                                               Pkg.instantiate()'
        - julia --project=docs/ docs/make.jl
      after_success: 
        - julia -e 'cd(Pkg.dir("Omniscape"));
                    Pkg.add("Coverage");
                    using Coverage;
                    Codecov.submit(Codecov.process_folder());
                    Coveralls.submit(Coveralls.process_folder())'
