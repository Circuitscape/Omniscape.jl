const REQUIRED_ARGS = ["resistance_file",
                       "radius",
                       "project_name"]

const TRUELIST = ["true", "True", "TRUE"]

const SINGLE = ["single", "Single", "SINGLE"]
